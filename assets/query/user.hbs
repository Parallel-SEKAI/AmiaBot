<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <style>
    :root {
      --primary: #6750A4;
      --on-primary: #FFFFFF;
      --primary-container: #EADDFF;
      --on-primary-container: #21005D;
      --surface: #FEF7FF;
      --on-surface: #1D1B20;
      --outline: #79747E;
      --divider: rgba(0, 0, 0, 0.1);
      --vip: #B3261E;
    }

    body {
      margin: 0;
      padding: 20px;
      font-family: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
      background-color: var(--primary-container);
      display: flex;
      justify-content: center;
    }

    #render-target {
      background-color: var(--surface);
      color: var(--on-surface);
      border-radius: 16px;
      padding: 24px;
      width: 500px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .header {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }

    .avatar {
      width: 80px;
      height: 80px;
      border-radius: 40px;
      object-fit: cover;
      margin-right: 20px;
      border: 2px solid var(--primary);
    }

    .header-info {
      display: flex;
      flex-direction: column;
    }

    .user-name {
      font-size: 24px;
      font-weight: bold;
      color: var(--on-surface);
    }

    .user-id {
      font-size: 16px;
      color: var(--outline);
    }

    .divider {
      height: 1px;
      background-color: var(--divider);
      margin: 16px 0;
    }

    .info-grid {
      display: grid;
      grid-template-columns: 100px 1fr;
      gap: 8px;
      font-size: 16px;
    }

    .info-label {
      color: var(--outline);
    }

    .info-value {
      color: var(--on-surface);
      word-break: break-all;
    }

    .footer {
      margin-top: 24px;
      padding-top: 12px;
      border-top: 1px solid var(--divider);
      font-size: 12px;
      color: var(--outline);
      text-align: right;
    }

    .status-vip {
      color: var(--vip);
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div id="render-target">
    <div class="header">
      <img src="{{avatarUrl}}" class="avatar" alt="Avatar">
      <div class="header-info">
        <div class="user-name">{{fullName}}</div>
        <div class="user-id">QQ号: {{id}}</div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="info-grid">
      {{#if sex}}
      <div class="info-label">性别:</div>
      <div class="info-value">{{sexText}}</div>
      {{/if}}

      {{#if age}}
      <div class="info-label">年龄:</div>
      <div class="info-value">{{age}}</div>
      {{/if}}

      {{#if regYear}}
      <div class="info-label">Q龄:</div>
      <div class="info-value">{{qAge}}年 ({{regYear}}年注册)</div>
      {{/if}}

      {{#if qqLevel}}
      <div class="info-label">QQ等级:</div>
      <div class="info-value">{{qqLevel}}</div>
      {{/if}}

      {{#if card}}
      <div class="info-label">群名片:</div>
      <div class="info-value">{{card}}</div>
      {{/if}}

      {{#if roleText}}
      <div class="info-label">群角色:</div>
      <div class="info-value">{{roleText}}</div>
      {{/if}}

      {{#if groupLevel}}
      <div class="info-label">群等级:</div>
      <div class="info-value">{{groupLevel}}</div>
      {{/if}}

      {{#if title}}
      <div class="info-label">群头衔:</div>
      <div class="info-value">{{title}}</div>
      {{/if}}

      {{#if joinTime}}
      <div class="info-label">加入时间:</div>
      <div class="info-value">{{joinTime}}</div>
      {{/if}}

      {{#if lastSentTime}}
      <div class="info-label">最后发言:</div>
      <div class="info-value">{{lastSentTime}}</div>
      {{/if}}

      {{#if hasVipInfo}}
      <div class="info-label">VIP状态:</div>
      <div class="info-value {{#if isVip}}status-vip{{/if}}">
        {{#if isVip}}是{{else}}否{{/if}}
      </div>

      <div class="info-label">年费VIP:</div>
      <div class="info-value {{#if isYearsVip}}status-vip{{/if}}">
        {{#if isYearsVip}}是{{else}}否{{/if}}
      </div>

      {{#if vipLevel}}
      <div class="info-label">VIP等级:</div>
      <div class="info-value">{{vipLevel}}</div>
      {{/if}}
      {{/if}}
    </div>

    <div class="footer">
      Generated by AmiaBot<br>
      Designed by Parallel SEKAI
    </div>
  </div>
</body>
</html>
