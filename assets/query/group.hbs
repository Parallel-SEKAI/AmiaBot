<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <style>
    :root {
      --primary: #6750A4;
      --on-primary: #FFFFFF;
      --primary-container: #EADDFF;
      --on-primary-container: #21005D;
      --surface: #FEF7FF;
      --on-surface: #1D1B20;
      --outline: #79747E;
      --divider: rgba(0, 0, 0, 0.1);
    }

    body {
      margin: 0;
      padding: 20px;
      font-family: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
      background-color: var(--primary-container);
      display: flex;
      justify-content: center;
    }

    #render-target {
      background-color: var(--surface);
      color: var(--on-surface);
      border-radius: 16px;
      padding: 24px;
      width: 500px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .header {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }

    .avatar {
      width: 80px;
      height: 80px;
      border-radius: 12px;
      object-fit: cover;
      margin-right: 20px;
    }

    .header-info {
      display: flex;
      flex-direction: column;
    }

    .group-name {
      font-size: 24px;
      font-weight: bold;
      color: var(--on-surface);
    }

    .group-id {
      font-size: 16px;
      color: var(--outline);
    }

    .divider {
      height: 1px;
      background-color: var(--divider);
      margin: 16px 0;
    }

    .info-row {
      display: flex;
      margin-bottom: 8px;
      font-size: 16px;
    }

    .info-label {
      width: 100px;
      color: var(--outline);
      flex-shrink: 0;
    }

    .info-value {
      color: var(--on-surface);
      word-break: break-all;
    }

    .section-title {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 8px;
      color: var(--primary);
    }

    .description {
      font-size: 16px;
      line-height: 1.5;
      white-space: pre-wrap;
    }

    .footer {
      margin-top: 24px;
      padding-top: 12px;
      border-top: 1px solid var(--divider);
      font-size: 12px;
      color: var(--outline);
      text-align: right;
    }

    .status-active {
      color: #B3261E;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div id="render-target">
    <div class="header">
      <img src="{{avatarUrl}}" class="avatar" alt="Avatar">
      <div class="header-info">
        <div class="group-name">{{name}}</div>
        <div class="group-id">群号: {{id}}</div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="info-section">
      {{#if level}}
      <div class="info-row">
        <div class="info-label">群等级:</div>
        <div class="info-value">{{level}}</div>
      </div>
      {{/if}}

      {{#if createTime}}
      <div class="info-row">
        <div class="info-label">创建时间:</div>
        <div class="info-value">{{createTime}}</div>
      </div>
      {{/if}}

      {{#if memberCount}}
      <div class="info-row">
        <div class="info-label">成员数量:</div>
        <div class="info-value">{{memberCount}}/{{maxMemberCount}}</div>
      </div>
      {{/if}}

      {{#if activeMemberCount}}
      <div class="info-row">
        <div class="info-label">活跃成员:</div>
        <div class="info-value">{{activeMemberCount}}</div>
      </div>
      {{/if}}

      {{#if ownerId}}
      <div class="info-row">
        <div class="info-label">群主ID:</div>
        <div class="info-value">{{ownerId}}</div>
      </div>
      {{/if}}

      {{#if rules}}
      <div class="info-row">
        <div class="info-label">群规:</div>
        <div class="info-value">{{rules}}</div>
      </div>
      {{/if}}

      {{#if joinQuestion}}
      <div class="info-row">
        <div class="info-label">加群问题:</div>
        <div class="info-value">{{joinQuestion}}</div>
      </div>
      {{/if}}

      <div class="info-row">
        <div class="info-label">全员禁言:</div>
        <div class="info-value {{#if isMutedAll}}status-active{{/if}}">
          {{#if isMutedAll}}开启{{else}}关闭{{/if}}
        </div>
      </div>
    </div>

    {{#if description}}
    <div class="divider"></div>
    <div class="section-title">群描述:</div>
    <div class="description">{{description}}</div>
    {{/if}}

    <div class="footer">
      Generated by AmiaBot<br>
      Designed by Parallel SEKAI
    </div>
  </div>
</body>
</html>
