<html lang='zh-CN'>
  <head>
    <meta charset='UTF-8' />
    <style>
      :root {
        --primary: #6750a4;
        --on-primary: #ffffff;
        --primary-container: #eaddff;
        --on-primary-container: #21005d;
        --surface: #fef7ff;
        --on-surface: #1d1b20;
        --outline: #79747e;
      }

      body {
        margin: 0;
        padding: 20px;
        font-family:
          'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', sans-serif;
        background-color: var(--primary-container);
        display: flex;
        justify-content: center;
      }

      #render-target {
        background-color: var(--surface);
        color: var(--on-surface);
        border-radius: 16px;
        padding: 24px;
        width: 600px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }

      .header {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
        border-bottom: 1px solid var(--outline);
        padding-bottom: 12px;
      }

      .logo {
        width: 48px;
        height: 48px;
        margin-right: 16px;
      }

      .title {
        font-size: 24px;
        font-weight: bold;
        color: var(--primary);
      }

      .content {
        line-height: 1.6;
        white-space: pre-wrap;
        font-size: 16px;
      }

      .footer {
        margin-top: 24px;
        padding-top: 12px;
        border-top: 1px solid var(--outline);
        font-size: 12px;
        color: var(--outline);
        text-align: right;
      }
    </style>
  </head>
  <body>
    <div id='render-target'>
      <div class='header'>
        <img src='assets://logo.svg' alt='Logo' class='logo' />
        <div class='title'>AmiaBot 帮助手册</div>
      </div>
      <div class='content'>{{{helpText}}}</div>
      <div class='footer'>
        Generated by AmiaBot<br />
        Designed by Parallel SEKAI
      </div>
    </div>
  </body>
</html>