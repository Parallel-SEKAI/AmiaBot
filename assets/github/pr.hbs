<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <style>
    :root {
      --primary: #24292f;
      --on-primary: #ffffff;
      --surface: #ffffff;
      --on-surface: #24292f;
      --outline: #d0d7de;
      --divider: #d8dee4;
      --text-secondary: #57606a;
      --bg: #f6f8fa;
      --open: #0969da;
      --merged: #8250df;
      --closed: #cf222e;
    }

    body {
      margin: 0;
      padding: 20px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
      background-color: var(--bg);
      display: flex;
      justify-content: center;
    }

    #render-target {
      background-color: var(--surface);
      color: var(--on-surface);
      border-radius: 12px;
      padding: 24px;
      width: 500px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
      border: 1px solid var(--outline);
    }

    .pr-header {
      display: flex;
      flex-direction: column;
      margin-bottom: 16px;
    }

    .pr-title-row {
      display: flex;
      align-items: flex-start;
      gap: 8px;
    }

    .pr-number {
      font-size: 20px;
      color: var(--text-secondary);
      font-weight: 300;
      flex-shrink: 0;
    }

    .pr-title {
      font-size: 20px;
      font-weight: 600;
      line-height: 1.25;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      font-size: 14px;
      font-weight: 500;
      border-radius: 2em;
      margin-top: 8px;
      color: white;
    }

    .status-open { background-color: var(--open); }
    .status-merged { background-color: var(--merged); }
    .status-closed { background-color: var(--closed); }

    .divider {
      height: 1px;
      background-color: var(--divider);
      margin: 16px 0;
    }

    .author-section {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
    }

    .author-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 12px;
      border: 1px solid var(--outline);
    }

    .author-name {
      font-size: 14px;
      font-weight: 600;
    }

    .created-at {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .branch-info {
      font-size: 14px;
      background-color: #eff1f3;
      padding: 8px 12px;
      border-radius: 6px;
      margin-bottom: 16px;
      font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;
    }

    .pr-body {
      font-size: 14px;
      line-height: 1.5;
      color: var(--on-surface);
      white-space: pre-wrap;
      word-break: break-word;
      max-height: 300px;
      overflow: hidden;
    }

    .stats-row {
      display: flex;
      gap: 20px;
      margin-top: 16px;
    }

    .stat-item {
      display: flex;
      flex-direction: column;
    }

    .stat-label {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .stat-value {
      font-size: 16px;
      font-weight: 600;
    }

    .diff-add { color: #1a7f37; }
    .diff-del { color: #cf222e; }

    .time-section {
      margin-top: 16px;
      font-size: 12px;
      color: var(--text-secondary);
    }

    .footer {
      margin-top: 24px;
      padding-top: 12px;
      border-top: 1px solid var(--divider);
      font-size: 12px;
      color: var(--text-secondary);
      text-align: right;
    }
  </style>
</head>
<body>
  <div id="render-target">
    <div class="pr-header">
      <div class="pr-title-row">
        <span class="pr-number">#{{number}}</span>
        <span class="pr-title">{{title}}</span>
      </div>
      <div class="status-badge {{#if isMerged}}status-merged{{else if isOpen}}status-open{{else}}status-closed{{/if}}">
        {{#if isMerged}}已合并{{else if isOpen}}开放{{else}}已关闭{{/if}}
      </div>
    </div>

    <div class="divider"></div>

    <div class="author-section">
      <img src="{{authorAvatarUrl}}" class="author-avatar" alt="Avatar">
      <div>
        <div class="author-name">{{authorLogin}}</div>
        <div class="created-at">发布于 {{createdAt}}</div>
      </div>
    </div>

    <div class="branch-info">
      {{headBranch}} → {{baseBranch}}
    </div>

    {{#if body}}
    <div class="pr-body">{{body}}</div>
    {{/if}}

    <div class="divider"></div>

    <div class="stats-row">
      <div class="stat-item">
        <div class="stat-label">评论</div>
        <div class="stat-value">{{comments}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">提交</div>
        <div class="stat-value">{{commits}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">添加</div>
        <div class="stat-value diff-add">+{{additions}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">删除</div>
        <div class="stat-value diff-del">-{{deletions}}</div>
      </div>
    </div>

    <div class="time-section">
      <div>最后更新: {{updatedAt}}</div>
      {{#if mergedAt}}<div>合并于: {{mergedAt}}</div>{{/if}}
    </div>

    <div class="footer">
      Generated by AmiaBot<br>
      Designed by Parallel SEKAI
    </div>
  </div>
</body>
</html>
