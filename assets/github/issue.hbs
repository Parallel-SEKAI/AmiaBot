<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <style>
    :root {
      --primary: #24292f;
      --on-primary: #ffffff;
      --surface: #ffffff;
      --on-surface: #24292f;
      --outline: #d0d7de;
      --divider: #d8dee4;
      --text-secondary: #57606a;
      --bg: #f6f8fa;
      --open: #1a7f37;
      --closed: #cf222e;
    }

    body {
      margin: 0;
      padding: 20px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
      background-color: var(--bg);
      display: flex;
      justify-content: center;
    }

    #render-target {
      background-color: var(--surface);
      color: var(--on-surface);
      border-radius: 12px;
      padding: 24px;
      width: 500px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
      border: 1px solid var(--outline);
    }

    .issue-header {
      display: flex;
      flex-direction: column;
      margin-bottom: 16px;
    }

    .issue-title-row {
      display: flex;
      align-items: flex-start;
      gap: 8px;
    }

    .issue-number {
      font-size: 20px;
      color: var(--text-secondary);
      font-weight: 300;
      flex-shrink: 0;
    }

    .issue-title {
      font-size: 20px;
      font-weight: 600;
      line-height: 1.25;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      font-size: 14px;
      font-weight: 500;
      border-radius: 2em;
      margin-top: 8px;
      color: white;
    }

    .status-open {
      background-color: var(--open);
    }

    .status-closed {
      background-color: var(--closed);
    }

    .divider {
      height: 1px;
      background-color: var(--divider);
      margin: 16px 0;
    }

    .author-section {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
    }

    .author-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 12px;
      border: 1px solid var(--outline);
    }

    .author-name {
      font-size: 14px;
      font-weight: 600;
    }

    .created-at {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .issue-body {
      font-size: 14px;
      line-height: 1.5;
      color: var(--on-surface);
      white-space: pre-wrap;
      word-break: break-word;
      max-height: 400px;
      overflow: hidden;
      position: relative;
    }

    .stats-row {
      display: flex;
      gap: 24px;
      margin-top: 16px;
    }

    .stat-item {
      display: flex;
      flex-direction: column;
    }

    .stat-label {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .stat-value {
      font-size: 16px;
      font-weight: 600;
    }

    .footer {
      margin-top: 24px;
      padding-top: 12px;
      border-top: 1px solid var(--divider);
      font-size: 12px;
      color: var(--text-secondary);
      text-align: right;
    }
  </style>
</head>
<body>
  <div id="render-target">
    <div class="issue-header">
      <div class="issue-title-row">
        <span class="issue-number">#{{number}}</span>
        <span class="issue-title">{{title}}</span>
      </div>
      <div class="status-badge {{#if isOpen}}status-open{{else}}status-closed{{/if}}">
        {{#if isOpen}}开放{{else}}关闭{{/if}}
      </div>
    </div>

    <div class="divider"></div>

    <div class="author-section">
      <img src="{{authorAvatarUrl}}" class="author-avatar" alt="Avatar">
      <div>
        <div class="author-name">{{authorLogin}}</div>
        <div class="created-at">发布于 {{createdAt}}</div>
      </div>
    </div>

    {{#if body}}
    <div class="issue-body">{{body}}</div>
    {{/if}}

    <div class="divider"></div>

    <div class="stats-row">
      <div class="stat-item">
        <div class="stat-label">评论</div>
        <div class="stat-value">{{comments}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">状态</div>
        <div class="stat-value" style="color: {{#if isOpen}}var(--open){{else}}var(--closed){{/if}}">
          {{#if isOpen}}开放{{else}}关闭{{/if}}
        </div>
      </div>
    </div>

    <div class="footer">
      Generated by AmiaBot<br>
      Designed by Parallel SEKAI
    </div>
  </div>
</body>
</html>
