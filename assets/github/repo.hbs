<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <style>
    :root {
      --primary: #24292f;
      --on-primary: #ffffff;
      --surface: #ffffff;
      --on-surface: #24292f;
      --outline: #d0d7de;
      --divider: #d8dee4;
      --text-secondary: #57606a;
      --bg: #f6f8fa;
    }

    body {
      margin: 0;
      padding: 20px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
      background-color: var(--bg);
      display: flex;
      justify-content: center;
    }

    #render-target {
      background-color: var(--surface);
      color: var(--on-surface);
      border-radius: 12px;
      padding: 24px;
      width: 500px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
      border: 1px solid var(--outline);
    }

    .repo-full-name {
      font-size: 20px;
      font-weight: 600;
      color: #0969da;
      margin-bottom: 8px;
    }

    .description {
      font-size: 14px;
      line-height: 1.5;
      color: var(--on-surface);
      margin-bottom: 16px;
    }

    .divider {
      height: 1px;
      background-color: var(--divider);
      margin: 16px 0;
    }

    .owner-section {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
    }

    .owner-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 12px;
      border: 1px solid var(--outline);
    }

    .owner-name {
      font-size: 14px;
      font-weight: 600;
    }

    .owner-type {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .stats-row {
      display: flex;
      gap: 24px;
      margin-bottom: 16px;
    }

    .stat-item {
      display: flex;
      flex-direction: column;
    }

    .stat-label {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .stat-value {
      font-size: 16px;
      font-weight: 600;
    }

    .info-grid {
      display: grid;
      grid-template-columns: 100px 1fr;
      gap: 8px;
      font-size: 14px;
    }

    .info-label {
      color: var(--text-secondary);
    }

    .info-value {
      color: var(--on-surface);
    }

    .time-info {
      margin-top: 16px;
      font-size: 12px;
      color: var(--text-secondary);
    }

    .footer {
      margin-top: 24px;
      padding-top: 12px;
      border-top: 1px solid var(--divider);
      font-size: 12px;
      color: var(--text-secondary);
      text-align: right;
    }
  </style>
</head>
<body>
  <div id="render-target">
    <div class="repo-full-name">{{fullName}}</div>
    <div class="description">{{description}}</div>

    <div class="divider"></div>

    <div class="owner-section">
      <img src="{{ownerAvatarUrl}}" class="owner-avatar" alt="{{ownerLogin}} avatar">
      <div>
        <div class="owner-name">{{ownerLogin}}</div>
        <div class="owner-type">{{ownerType}}</div>
      </div>
    </div>

    <div class="stats-row">
      <div class="stat-item">
        <div class="stat-label">星标</div>
        <div class="stat-value">{{stars}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">分支</div>
        <div class="stat-value">{{forks}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">问题</div>
        <div class="stat-value">{{issues}}</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">观察</div>
        <div class="stat-value">{{watchers}}</div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="info-grid">
      <div class="info-label">主要语言:</div>
      <div class="info-value">{{language}}</div>

      <div class="info-label">许可证:</div>
      <div class="info-value">{{license}}</div>

      <div class="info-label">默认分支:</div>
      <div class="info-value">{{defaultBranch}}</div>

      <div class="info-label">可见性:</div>
      <div class="info-value">{{visibility}}</div>
    </div>

    <div class="time-info">
      <div>创建时间: {{createdAt}}</div>
      <div>最后更新: {{updatedAt}}</div>
    </div>

    <div class="footer">
      Generated by AmiaBot<br>
      Designed by Parallel SEKAI
    </div>
  </div>
</body>
</html>
